'use client'
import type { ReactNode } from 'react'

export function QueryProvider({ children }: { children: ReactNode }) {
  // Replace with TanStack Query provider later
  return <>{children}</>
}

export default QueryProvider
