'use client'
import type { ReactNode } from 'react'

export function ThemeProvider({ children }: { children: ReactNode }) {
  // Replace with theming solution (e.g., next-themes) later
  return <>{children}</>
}

export default ThemeProvider
